# Tasks — Item #13: Coffee Shop Theme Module

## Steps

- [ ] Write failing test: `tests/unit/coffee-shop.test.ts`
  - Verify: `npm test` shows 6+ failing assertions
- [ ] Implement `src/themes/coffee-shop.ts`
  - Verify: TypeScript compiles (`npm run build`)
- [ ] Register theme in `src/themes/index.ts`
  - Verify: `getTheme('coffee-shop')` returns the theme
- [ ] Run full test suite
  - Verify: `npm test` all pass, including 100-seed generator run
- [ ] Run lint
  - Verify: `npm run lint` exits 0

## AI steps (require judgment)
- Suspect name selection (café-appropriate, 12+)
- Victim name selection (start with V, 6+)
- Clue template wording (café flavor)
- Narrative text variants (intro, victimFound, guiltyText)

## Command steps (deterministic)
- Import and export wiring in `index.ts`
- `registerTheme()` call
- TypeScript type annotations
